server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rajib821/Interview-Prep.git
          default-label: main
          clone-on-start: true
          search-paths: myConfig
